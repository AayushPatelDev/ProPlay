# Runtime dependencies for the API server (this is what Vercel installs).
# Kept slim: predictions are served from artifacts/model.json, so no scikit-learn/scipy.
# For fetching data and training, install requirements-train.txt instead.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-dotenv>=1.0
pandas>=2.2
numpy>=1.26
